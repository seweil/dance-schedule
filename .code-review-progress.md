# Full-repo code review — progress tracker

Committed to git (2026-09-07, at the user's request — originally a local-only
working file, see the git log for why). Restart point if a session hits its
usage cap mid-run: re-open this file, find the next `[ ]` chunk, and continue
with `/code-review max <path>` (or ask Claude to "resume the code review").
Findings are also published as `docs/dance-schedule-audit.html`, referenced
from `docs/known-issues.md`.

## Fix progress (started 2026-09-07)

Prioritizing by impact on the current live event (`MotivateToSeattle`, per
`content/config.yaml`) over hypothetical future events, per user direction.

- [x] **Manifest description TODO placeholder** (PWA config chunk) — fixed in
  `cc01df5`: made `manifest.description` configurable per content set like
  `name`/`shortName`, real default + real value set for MotivateToSeattle.
  Verified live in a real build's `manifest.webmanifest`.
- [x] **Text-size preference FOUC** (src/hooks chunk) — fixed in `cc01df5`:
  switched `useTextSizePreference.ts` from `useEffect` to `useLayoutEffect`.
- [x] **Browser tab title hardcoded "Dance Schedule"** (PWA config chunk,
  found during triage, not originally in the "start here" list) — fixed in
  `5339c24`: per-content-set via a new transformIndexHtml plugin, verified
  live in a real build + new e2e assertion.
- [x] **e2e false-pass test** (e2e chunk) — fixed in `816aa91`: both
  assertions now match `[class*="card"]` instead of the first list item
  (the date heading). Verified live against a real build.
- [x] **docs/ drift, all 9 findings** (docs chunk) — fixed in `717a12d`:
  ops.md, design/monitoring.md, design/content-config.md,
  design/onboarding-hints.md, design/dance-schedule-mobile-scroll.md (x2),
  design/schedule-page.md + adding-a-new-event.md (meridiem, both docs),
  testing.md (x2). Superseded sections marked rather than rewritten,
  matching this repo's existing convention (see e.g. monitoring.md's own
  "Fixed and verified" annotations).
- [x] **Four cheap, generally-correct bugs** — fixed in `8ed811d`:
  ScheduleList.tsx duplicate key, ZoomableImage.tsx prototype-pollution
  (`in` → `Object.hasOwn`), parseDanceScheduleSheet.ts room dedup (mirrors
  the existing caller dedup), computeDanceScheduleCallerLayout.ts same-start
  clip bug (floored at 1 row). All with new/updated test coverage.
- [x] **Five tooling gaps** — fixed in `a0480a4`: tsconfig.json missing 3
  root test files from typecheck, eslint.config.js giving scripts/*.mjs zero
  real rules, package.json's pnpm version mismatch vs. CI/Amplify (re-pinned
  to 11.15.1, hash independently verified against the real tarball),
  amplify.yml's stale "real" example, CLAUDE.md's missing test:coverage
  command.
- [ ] Route collision guard (src/pages chunk) — checked: not currently
  triggered by any real content set's page names. Defensive fix, not urgent.
- [ ] Config silent-mask pattern (content config chunk, 5 findings) — checked:
  no content set's config.yaml currently has a malformed/blank value that
  would trigger this. Defensive fix, not urgent.
- [ ] Non-square icon crop (PWA config chunk) — checked: MotivateToSeattle's
  icon.png is already a clean 1024x1024 square. Defensive fix, not urgent.
- [ ] `RESERVED_NAMES` missing "reset" (scripts chunk) — only matters for a
  future content set literally named "reset". Not urgent.
- [ ] `__root__` collision, unescaped-regex-from-set-name (x3: scripts,
  e2e, root-config) — all defensive, same underlying "content-set name
  isn't validated as regex/path-safe" gap. Not urgent.
- [ ] Everything else in the audit not listed above — untouched so far:
  RawDanceScheduleTable's unused `groupBoundary`, MdxH1/MdxA prop-spreading
  latent bugs, UpdatePrompt listener leak, BUILD_TIME per-subprocess
  inconsistency, the three duplicated `TIME_COLUMN_WIDTH`s, the duplicated
  e2e setup blocks, the content-config parse-error duplication, missing
  test coverage for `useDismissableMenu.ts`/`assignLanes.ts`/
  `UpdatePrompt.tsx`, CSS-module substring e2e locators, efficiency nits
  (getSnapshot memoization, buildNavTree hoist, icon gen parallelization,
  4x config re-parse, sequential builds) — see the full findings above for
  the complete list.

Each `/code-review max` invocation for the whole repo kept exhausting the
session budget before finishing (multi-agent "max" spawns ~10 finder agents
per run). Splitting by directory/topic didn't fix it alone — even scoped to
just `src/lib`, `max` still fanned out ~10 parallel finder agents and all of
them hit the session cap at once (2026-09-07 attempt). The parallelism
itself is what exhausts the budget, not repo scope. **Switched to `high`
effort per chunk** (fewer/lighter parallel agents) starting with the retry
of `src/lib` below — trading some depth for actually finishing.

## Chunks

- [x] CLAUDE.md conventions audit (ad hoc angle from first attempt) — findings recorded below
- [x] Wrapper/delegation correctness (ad hoc angle from first attempt) — findings recorded below
- [x] src/lib (parsing/utilities — highest correctness risk) — done at `high` effort, findings below
- [x] src/hooks — done at `high` effort, findings below
- [x] src/components — done at `high` effort, findings below
- [x] src/pages + App.tsx + routing — done at `high` effort, findings below
- [x] src/sw + PWA config (vite.config.ts, manifest, service worker, content-icons.ts) — done at `high` effort (no src/sw dir exists; uses generateSW), findings below
- [x] scripts/ (build-content-sets.mjs, edit-test-data.mjs, generate-amplify-rewrites.mjs) — done at `high` effort, findings below
- [x] e2e/ — done at `high` effort, findings below
- [x] docs/ (design docs, adding-a-new-event.md, testing.md, ops.md) — accuracy vs. code — done at `high` effort, findings below
- [x] content/ pipeline config (config.yaml schemas across sets — not markdown prose) — done at `high` effort, findings below
- [x] root configs (package.json, tsconfig, eslint config, vite.config.ts overall) + CLAUDE.md itself — done at `high` effort, findings below

## ALL CHUNKS COMPLETE (2026-09-07)

## Findings banked so far

### CLAUDE.md conventions audit

1. `src/components/StickyScrollGrid.tsx` — imports CSS classes from `DanceScheduleGrid.module.css` instead of its own colocated module; no `StickyScrollGrid.module.css` exists.
2. `content/MotivateToSeattle/pages/22 Restaurants.md` — filename not kebab-case (capitalized), unlike every sibling.
3. `src/components/UpdatePrompt.tsx` — no colocated test file, unlike its 3 sibling ephemeral-UI-chrome examples named in CLAUDE.md.
4. `.github/workflows/ci.yml:81` — `npx playwright install --with-deps chromium` instead of pnpm-based invocation like every other CI step.
5. `src/index.css:78-97` — table styling rules are component-specific, not font-stack/reset/token, contra CLAUDE.md's stated scope for that file.
6. `src/types/virtual-*.d.ts` (4 files) — ambient module declarations use `export default`, an undocumented second exception to the named-exports rule beyond the one documented route-file exception.

### Wrapper/delegation correctness

Areas verified clean: appStorage.ts, resetAppState.ts, rum.ts, ImageGallery/useImageGallery, text size context — no bypasses found.

1. `src/lib/mailtoDiagnostics.ts:58` — reads `navigator.onLine` directly instead of the reactive `useOnlineStatus()` hook `BuildInfo.tsx` uses for the same purpose; a mailto body can bake in a stale "Online" status if connectivity drops after render. High confidence.
2. `src/App.tsx:31-33` (`MdxH1`) — drops all props except `children`, unlike sibling `img`/`a` overrides which spread `...rest`. Currently latent (no plugin attaches heading props today) but would silently break heading IDs if e.g. `rehype-slug` were added later. Medium confidence.
3. `src/App.tsx:62-71` (`MdxA`, mailto branch) — `{...rest}` spreads after the tracking `onClick`, so a future caller-supplied `onClick` would silently clobber RUM tracking. Not reachable today (plain markdown can't produce this). Low confidence / speculative.

### src/lib (high effort)

1. `src/lib/parseDanceScheduleSheet.ts:512` — room-booking conflict check doesn't dedupe `session.location.rooms` the way the caller-booking loop just above it (`:492`) explicitly does with `new Set(session.callers)`. A "ROOMS:" line listing the same room twice produces a nonsensical self-referential "already booked" build error, or (if that check were skipped) a duplicate rendered card. High confidence.
2. `src/lib/computeDanceScheduleCallerLayout.ts:241` — `clipFreeFloatingEntries` only clips a "free" entry when another entry starts *strictly after* it, so a same-start neighbor never triggers a clip; the free card renders full-width overlapping a coincident real session. An existing test (`computeDanceScheduleCallerLayout.test.ts:420-433`) exercises this exact case but only asserts lane/columnSpan, not rowSpan, so it doesn't catch the gap. High confidence.
3. `src/lib/mailtoDiagnostics.ts:58` — reconfirms the same `navigator.onLine` vs. reactive `useOnlineStatus()` finding from the wrapper/delegation angle above.
4. `src/lib/assignLanes.ts` — the shared greedy lane-assignment algorithm used by both dance-schedule layout files has no colocated unit test, unlike nearly every other file in the directory; only exercised indirectly through two larger consumer suites.

### src/hooks (high effort)

1. `src/hooks/useTextSizePreference.ts:39` — applies the stored text-size preference via a plain `useEffect` (post-paint) instead of `useLayoutEffect`. A returning visitor with a non-default size sees a visible flash to normal size before it corrects — the same class of bug this repo already fixed elsewhere with `useLayoutEffect` (per `docs/design/text-size-preference.md`), just not applied here. High confidence, user-visible.
2. `src/hooks/useLastPagePersistence.ts:31` — `isInitialRender` guard is a per-instance ref, not module-level, so StrictMode's dev-only double-invoke re-enters the fallback branch and clobbers the just-read redirect target back to `/` in storage. Self-corrects on the real navigation; dev-only/transient.
3. `src/hooks/useDismissableMenu.ts` — no colocated test file, unlike 14 of the other 16 hooks, despite owning real interaction logic (Escape-to-close, outside-pointerdown, dual root/portal containment checks) used by both `Nav.tsx` and `PageMenu.tsx`.
4. `src/hooks/useFirstLaunchHint.ts:105` — `getSnapshot` passed to `useSyncExternalStore` is a fresh closure every render instead of stable (unlike `subscribe` just above it), causing extra defensive re-invocation/localStorage reads on hot-render paths like the dance-schedule filter slider. Minor efficiency, not a correctness bug.
5. `src/hooks/useFirstLaunchHint.ts:25` — module-level `subscribers` Map never removes an id's entry once its Set is empty; unbounded-in-principle but low impact today (small fixed id set).

### src/components (high effort)

1. `src/components/ScheduleList.tsx:34` — list key `event.startTime.toISOString()` isn't unique when two events share a start time; verified live via the component's own test fixture, which already triggers React's duplicate-key warning.
2. `src/components/RawDanceScheduleTable.tsx:82` — `HourSummaryTable` never reads `groupBoundary`, so the debug "Hours by caller" table can't show the headline-vs-GCA-showcase split the data was computed to support.
3. `src/components/ZoomableImage.tsx:56` — `token in SIZE_CLASSES` matches inherited `Object.prototype` properties (e.g. `"constructor"`), not just the six declared size keywords; a caption title containing such a word is misclassified and silently dropped. Verified via node repro.
4. `src/components/DanceScheduleGrid.tsx:23` — `TIME_COLUMN_WIDTH` (with its rationale comment) is duplicated verbatim across `DanceScheduleGrid.tsx`, `DanceScheduleLevelGrid.tsx`, `DanceScheduleCallerGrid.tsx` instead of living in `StickyScrollGrid.tsx`, the trio's existing shared shell.
5. `src/components/DanceScheduleGrid.tsx:78` — the "No sessions match... Show all levels" empty-state block is copy-pasted near-identically across the same three grid components.

(Note: `StickyScrollGrid.tsx` importing `DanceScheduleGrid.module.css` instead of its own module was already banked above under CLAUDE.md conventions — not repeated here.)

### src/pages + App.tsx + routing (high effort)

1. `src/App.tsx:89` — no collision guard between content-page routes and hardcoded utility routes (`/events`, `/debug`, `/clear-storage`, `/reset`) or reserved schedule slugs. A content page named e.g. `events.md` would silently shadow the real feature (react-router picks array order on tie) with no build error — inconsistent with how loudly this codebase guards other duplicate/collision cases elsewhere (`assertContentSetExists`, duplicate-header-cell errors, `deriveRoomOrder`'s duplicate-room check). High confidence, currently latent.
2. `src/App.tsx:31` — `MdxH1` wraps every rendered markdown `<h1>` in a full `PageHeader`/`PageMenu` with no guard against multiple top-level headings on one page; a page with two `# ` lines would render two duplicate "Site navigation" landmarks. No current content page triggers this (verified via grep) but nothing enforces or warns about it.
3. `src/components/Nav.tsx:17` — `buildNavTree(routes)` is recomputed from scratch on every render in both `Nav.tsx` and `PageMenu.tsx` even though `routes` is a build-time-static array; wasted work on a hot render path (resize/scroll-driven re-renders), not memoized or hoisted to module scope. Low impact given small route count.

### src/sw + PWA config (high effort)

1. `vite.config.ts:198` — PWA manifest `description` is a hardcoded literal TODO placeholder. Verified live: `dist/manifest.webmanifest` for every content set, including a real event (`backtrack2abq`), ships `"TODO: one-line description..."` verbatim. High confidence, user-visible.
2. `content-icons.ts:37` — `loadSourceIcon` validates minimum size but never validates the source icon is square, so a non-square `icon.png` is silently center-cropped by sharp's default `cover` fit in every generated size. Verified via direct sharp repro.
3. `vite.config.ts:36` — `BUILD_TIME` is computed fresh per `vite.config.ts` eval; `build-content-sets.mjs` runs one `vite build` subprocess per set sequentially with no shared timestamp forwarded, so sets from the same release show different build times in the diagnostics UI.
4. `index.html:15` — document `<title>` is hardcoded "Dance Schedule" for every content set, never customized per event, despite the manifest name/icon already being made per-set to fix identity confusion — the browser tab (pre-install) still shows the generic title.
5. `content-icons.ts:35` — a malformed/non-image `icon.png` throws a raw sharp internal error instead of a clear named error, breaking this codebase's usual fail-loud-with-actionable-message convention (contra `assertContentSetExists` elsewhere).
6. `content-icons.ts:60` — the three icon-generation steps (icon-192, icon-512, maskable-512) run as sequential awaits despite being independent; `Promise.all` would parallelize. Minor efficiency, compounds across N+1 sequential build subprocesses.
7. `src/components/UpdatePrompt.tsx:80` — `onRegisteredSW`'s `visibilitychange` listener + `setInterval` have no cleanup/removal path; latent leak if the callback ever fires more than once (currently masked since `UpdatePrompt` mounts once for the app's lifetime).
8. `content-icons.ts:42` — `loadSourceIcon` reads the source file from disk twice (`sharp().metadata()` then `fs.readFileSync`) instead of reusing one buffer. Minor efficiency.

### scripts/ (high effort)

1. `scripts/build-content-sets.mjs:38` — `RESERVED_NAMES` omits `"reset"`, even though `/reset` is a real hardcoded route in `src/App.tsx`; a content set literally named "reset" would silently shadow it in production with no build error. Both `docs/adding-a-new-event.md` and `docs/design/content-sets.md` list the same stale reserved-names set, confirming the omission wasn't intentional (relates to finding #1 in the src/pages chunk — same class of missing collision guard). High confidence.
2. `scripts/build-content-sets.mjs:109` — per-set staging `outDir` collides with the literal `__root__` directory used for the default set's extra unprefixed build, if any content set is ever named `"__root__"`; would crash the publish with a confusing `ENOENT` instead of a named validation error.
3. `scripts/generate-amplify-rewrites.mjs:44` — content-set name is interpolated unescaped into a regex-syntax Amplify rewrite rule with no validation it's regex-safe; nothing in the pipeline actually enforces the "lowercase letters, digits, hyphens only" convention docs describe (already violated today by `content/MotivateToSeattle`).
4. `scripts/build-content-sets.mjs:108` — the N+1 `vite build` subprocesses run strictly sequentially via `execFileSync` despite targeting fully isolated `outDir`s; publish time grows linearly with content-set count for no correctness reason.

### e2e/ (high effort)

1. `e2e/app.spec.ts:118` — "nav links to schedule page, which renders events" asserts `getByRole('listitem').first()` is visible, but the first `<li>` per date group is the date-heading, not an event card — the same file's own later test explicitly avoids this exact locator for this exact reason. Would false-pass even if all event cards failed to render. Same bug repeats at line 133. High confidence, real false-pass.
2. `e2e/content-sets.spec.ts:29` — `defaultContentSet` interpolated unescaped into `new RegExp(...)`; a set name with a regex metacharacter (kebab-case isn't actually enforced — `MotivateToSeattle` already violates it) could throw or match wrong.
3. `e2e/app.spec.ts:62` — the SW-ready/reload/offline-toggle sequence is duplicated near-verbatim 4x across `app.spec.ts`/`content-sets.spec.ts`/`room-schedule.spec.ts`; past this project's own "3 is fine, 4th needs a helper" threshold.
4. `e2e/app.spec.ts:152` — the horizontal-overflow check (`scrollWidth <= clientWidth`) is copy-pasted 5x across the same files.
5. `e2e/app.spec.ts:182` — the `devices['iPhone 13']` destructure + `test.use()` pattern duplicated 3x.
6. `e2e/room-schedule.spec.ts:10` — nearly every locator across all specs reaches into CSS-Module class names via substring match (`[class*="card"]` etc.) instead of `data-testid`; a substring collision already nearly exists today (only avoided because `storageState` pre-dismisses a prompt with a colliding class name).
7. `e2e/app.spec.ts:140` — stale comment claims a 640px breakpoint that `src/breakpoints.css` now defines as 641px; doesn't affect current test outcomes but could mislead a future viewport choice.

### docs/ (high effort) — all findings are stale-doc-vs-current-code drift

1. `docs/ops.md:108` — claims `initRum` calls a separate `addSessionAttributes()`; code now sets `sessionAttributes` in the constructor config instead (rum.ts comment explains why).
2. `docs/design/monitoring.md:208` — still claims several deploy/alarm scripts "need a human's local, currently-root credentials," but `docs/known-issues.md` marks this fixed 2026-08-26 via a scoped IAM user — and monitoring.md was itself edited *after* that fix, so it's not just pre-dating the change.
3. `docs/design/content-config.md:58` — says the fallback default content set is `'real'`; code (`content-config.ts:6`) defines it as `'automated-testing'`, and `content/real/` doesn't exist (renamed, per `content-sets.md`'s own correct account of the same rename — the two design docs now contradict each other).
4. `docs/design/onboarding-hints.md:440` — describes screen-dimming via a full-viewport `.backdrop` div; that was replaced by a per-target `box-shadow` technique specifically because the backdrop hid the highlighted control (per `HintBalloon.module.css`'s own comment). 5+ later cross-references repeat the stale claim.
5. `docs/design/dance-schedule-mobile-scroll.md:278` — says room columns use a fixed `150px` in `DanceScheduleGrid.tsx`; it's now a rem-based `ROOM_COLUMN_WIDTH_REM` in `computeDanceScheduleLayout.ts`, deliberately changed to scale with the text-size preference feature.
6. `docs/design/dance-schedule-mobile-scroll.md:99` — walks through scroll-sync logic as inline in `DanceScheduleGrid.tsx`; it was extracted into `useSyncedGridScroll.ts`/`StickyScrollGrid.tsx` per `dance-schedule.md`'s own later decision — the two docs now disagree on where the logic lives.
7. `docs/design/schedule-page.md:171` + `docs/adding-a-new-event.md:220` — both document meridiem inference as one-directional (start omits AM/PM), but `parseTimeRange.ts` and its tests also support the reverse direction (end omits AM/PM) — a real, tested, undocumented shorthand.
8. `docs/testing.md:15` vs `:84` — summary table says "3 e2e spec files" and "two root-level exceptions" to colocated tests; actual counts are 5 e2e specs and 3 root-level exceptions (missing `vite-plugin-content-config.test.ts`), contradicting the doc's own later, correct section.
9. `docs/testing.md:96` — "Known gaps" claims `BuildInfo.tsx` has no unit test; `BuildInfo.test.tsx` exists and is substantive (87 lines).

### content/ config schema (high effort) — mostly a silent-fallback pattern

1. `vite-plugin-content-config.ts:127` — `features`/`danceSchedule` YAML sections are cast to `Record<string, unknown>` without checking they're actually a mapping; a scalar value (e.g. `features: false`) silently falls through to defaults with no build error. Verified via repro.
2. `content-config.ts:69` — `assertContentSetExists`/`contentSetDir` don't reject an empty-string content-set name (`path.resolve(root,'content','')` collapses to `content/` itself, which exists) — `defaultContentSet: ""` would pass validation and fail later with a confusing raw ENOENT instead of the named error this function exists to give. Verified via repro.
3. `vite-plugin-content-config.ts:24` — boolean/string leaf defaults use `?? default`, which also masks an explicit YAML `null` (key present, value blank — e.g. a mid-edit save) as absent, silently applying the default. Same pattern at `content-config.ts:120-121` and `:156`.
4. `content-config.ts:123` — `manifest.name`/`shortName` validated only for `typeof === 'string'`, not non-empty; an explicit empty string ships as the PWA app name with no error.
5. `content-config.ts:40` — `loadTopLevelContentConfig`'s early-return path (config.yaml missing) skips `assertContentSetExists` for the hardcoded default-set fallback, unlike every other path through the function — inconsistent enforcement.
6. `vite-plugin-content-sets.ts:89` — the same `content/<set>/config.yaml` is read+YAML-parsed up to 4 times per set per build across different functions; no shared parse/cache.
7. `content-config.ts:110` — the "read file, parse YAML, catch-and-rethrow" block is duplicated verbatim 4 times across two files.
8. `vite-plugin-content-config.ts:100` — `DANCE_SCHEDULE_ROOM_ORDER` comma-parsing silently drops empty entries (double/trailing commas) instead of erroring, unlike the all-empty case which does throw.

### root configs + CLAUDE.md (high effort)

1. `tsconfig.json:22` — `include` omits the three root-level test files (`content-config.test.ts`, `content-icons.test.ts`, `vite-plugin-content-config.test.ts`); `tsc --noEmit` never type-checks them (verified via `--listFiles`), so a type error in any of them passes `pnpm typecheck`/`pnpm build`/pre-commit silently.
2. `package.json:67` — `packageManager` field pins pnpm 11.18.0, but CI and `amplify.yml` both pin 11.15.1 via `corepack prepare`, and `ci.yml`'s own comment incorrectly claims these match. A contributor doing plain `corepack enable && pnpm install` gets a different pnpm than CI/prod.
3. `eslint.config.js:11` — all real rule sets are scoped to `**/*.{ts,tsx}`, so `scripts/*.mjs` get zero active lint rules despite `pnpm lint` scanning them (verified via `--print-config`, all severities 0).
4. `amplify.yml:24` — custom-headers comment gives `dist/real/index.html` as an example path; the `real` set was renamed away and no longer exists — same drift already found in `docs/design/content-config.md`.
5. `vite.config.ts:242` — `navigateFallbackDenylist` builds `new RegExp` from content-set directory names with no escaping, for the production default-set SW build; same unescaped-regex-from-set-name class of bug as the Amplify rewrite finding in the scripts/ chunk.
6. `CLAUDE.md:29` — Commands section omits `pnpm test:coverage`, which CI's "Unit tests (with coverage)" step actually runs.
