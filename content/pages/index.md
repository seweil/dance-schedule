# Welcome to Dance Schedule

This is the home page. It's rendered from `content/pages/index.md` — pages and the
navigation menu are generated automatically from the markdown files in
`content/pages/`.

Add or edit files under `content/pages/` and the site's pages and menu update
accordingly, both in development and in the production build.
