# Per-content-set config for the "backtrack2abq" event — see
# docs/design/content-config.md.

features:
  # Both true — the recommended default per docs/adding-a-new-event.md (also this
  # repo's built-in default when a config.yaml omits `features` entirely — spelled
  # out explicitly here anyway for clarity alongside `manifest` below).
  combineA1A2: true
  combineC3BC4: true

manifest:
  name: Back Track 2 ABQ (44th IAGSDC Convention)
  shortName: BT2ABQ
