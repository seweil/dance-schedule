# About this content set

This content set exists purely as a stable, edge-case-rich fixture for
development and testing — it isn't tied to any real event, so it's safe to
extend whenever the schedule parsers gain support for a new format. Run it
with `pnpm dev:test` or `pnpm build:test`.
