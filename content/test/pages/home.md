# TEST CONTENT SET

This is the **`test`** content set (`content/test/`), active because
`CONTENT_SET=test` was set when this build or dev server started. If you're
seeing this, you are **not** looking at the real production content.

See [Edge Cases](/edge-cases) for what this fixture deliberately covers, and
`docs/design/content-sets.md` for how content sets work in general.
