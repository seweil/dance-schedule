# Per-content-set feature flags — read at build time and exposed to client-side
# code via the virtual:content-config module (vite-plugin-content-config.ts).
# See docs/design/content-config.md.

features:
  # true here (unlike content/real/config.yaml) specifically so the "test"
  # content set exercises the combined-slot behavior — see
  # docs/design/dance-schedule.md's LevelSlot decision.
  combineA1A2: true
